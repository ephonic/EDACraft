# EarphoneTop - L5 DSL Test Plan

Run `python -m pytest earphone/top/layer_L5_dsl/tests -q`.

Tests validate that the compatibility wrapper instantiates the real top-level
DSL and exposes required external ports.
