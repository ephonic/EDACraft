"""
skills — Domain-specific Spec2RTL extensions.

Each sub-package provides behavioral models, architecture templates,
behavior templates, skeleton steps, and DSL module implementations
for a particular design domain (CPU, GPGPU, NPU, DSP, NoC, codecs,
interfaces, memory controllers, etc.)
"""
