# ImplCraft 硬核能力增强计划 (2026-06-26)

## 概述

本计划为 ImplCraft IC 后端系统增加 6 项硬核能力，使其具备工业级大规模 SoC 设计的完整支持。

---

## 1. Power Mesh 构建 (src/power/mesh_builder.py)

**目标**: 自动生成 Power/Ground mesh 结构（rings + straps + rails），支持多电压域

**现有基础**:
- `src/analysis/pg_network_advisor.py` - PG pad 规划
- `src/pg_network.py` - PG 网络 CLI

**需要新增**:
- Power ring 生成（core ring + block ring）
- Power strap 生成（horizontal + vertical straps）
- Standard cell power rails 连接
- 多电压域支持（VDD_CORE, VDD_IO, VDD_MEM 等）
- IR-drop 预估与 mesh 密度优化
- Tcl 脚本生成（ICC2/Innovus 格式）

**关键算法**:
```python
class PowerMeshBuilder:
    def build_mesh(self, die_area, core_area, power_domains):
        # 1. Generate core rings (top/bottom/left/right)
        # 2. Generate block rings around hard macros
        # 3. Generate horizontal straps (evenly spaced)
        # 4. Generate vertical straps (evenly spaced)
        # 5. Connect to standard cell rails
        # 6. Optimize mesh density for IR-drop target
        pass
```

**交付物**:
- `src/power/mesh_builder.py` (~500 lines)
- `src/power/mesh_config.py` (~150 lines)
- `tests/test_power_mesh.py` (~300 lines)
- 示例配置: `configs/FullSystem/power_mesh.yaml`

---

## 2. 大规模设计拆分 Harden (src/analysis/harden_engine.py)

**目标**: 自动识别可 harden 的模块，生成 sub-block P&R 流程

**现有基础**:
- `src/analysis/partition_engine.py` - 基础分区决策
- `src/analysis/partition_orchestrator.py` - 分区编排器

**需要增强**:
- Harden 候选评分系统（timing criticality + connectivity + macro content）
- Sub-block interface 自动生成（wrapper RTL + SDC）
- Block-level P&R 脚本生成
- Top-level integration 脚本生成
- Block abstraction (blackbox) 管理

**关键算法**:
```python
class HardenEngine:
    def identify_harden_candidates(self, graph: ModuleGraph):
        # Score each module based on:
        # - Gate count (100K - 2M gates ideal)
        # - Timing criticality (low = good candidate)
        # - Cross-module signals (< 500 = good)
        # - Macro content (has SRAM/analog = good)
        pass
    
    def generate_block_flow(self, block_name, block_config):
        # Generate complete sub-block P&R flow:
        # - Block-level synthesis script
        # - Block-level P&R script (ICC2/Innovus)
        # - Block abstraction (LEF/DB) generation
        # - Timing budget allocation
        pass
```

**交付物**:
- `src/analysis/harden_engine.py` (~600 lines)
- `src/analysis/block_interface.py` (~300 lines)
- `tests/test_harden_engine.py` (~400 lines)
- 文档: `docs/HARDEN_METHODOLOGY.md`

---

## 3. 芯片面积规划 (src/analysis/area_planner.py)

**目标**: 根据 gate count + macro count + I/O count 自动估算 die size

**现有基础**:
- `src/analysis/floorplan_advisor.py` - 基础 floorplan 建议

**需要新增**:
- Die size 估算算法（基于 gate density + utilization）
- Aspect ratio 优化（考虑 routing congestion）
- Core offset 计算（考虑 I/O ring + seal ring）
- Multiple die size 候选方案
- Area budget breakdown（logic + macro + routing + I/O）

**关键算法**:
```python
class AreaPlanner:
    def estimate_die_size(self, gate_count, macro_area, io_count):
        # 1. Calculate logic area = gate_count * avg_gate_area
        # 2. Add macro area (SRAM + analog)
        # 3. Apply target utilization (0.6 - 0.8)
        # 4. Add routing overhead (1.2 - 1.5x)
        # 5. Calculate core area
        # 6. Add I/O ring width (typically 100-200um per side)
        # 7. Add seal ring (10-20um per side)
        # 8. Optimize aspect ratio for routing
        pass
    
    def generate_area_budget(self, die_size, blocks):
        # Breakdown area allocation:
        # - Logic area
        # - Macro area (SRAM, analog, IP)
        # - Routing area (estimated from congestion)
        # - I/O area
        # - Power mesh area
        # - Margin (10-15%)
        pass
```

**交付物**:
- `src/analysis/area_planner.py` (~400 lines)
- `tests/test_area_planner.py` (~250 lines)
- 集成到 floorplan advisor

---

## 4. Pad 规划摆放 (src/analysis/pad_planner.py)

**目标**: 自动规划 I/O pad、PG pad、bond pad 的摆放策略

**现有基础**:
- `src/analysis/pg_network_advisor.py` - PG pad 规划（部分实现）

**需要增强**:
- I/O pad 分组策略（按功能：DDR, PCIe, GPIO, JTAG 等）
- Timing-driven pad placement（critical signals 靠近 core）
- Signal integrity 约束（separation between analog/digital）
- Pad ring 自动生成
- Bond pad vs flip-chip bump 支持
- Pad assignment 验证（power/ground ratio, ESD 规则）

**关键算法**:
```python
class PadPlanner:
    def plan_io_pads(self, io_list, die_perimeter, constraints):
        # 1. Group pads by function/interface
        # 2. Assign to sides (N/S/E/W) based on:
        #    - Signal type (clock on short side)
        #    - Timing criticality (critical near center)
        #    - SI constraints (separate analog/digital)
        # 3. Optimize placement within each side
        # 4. Insert PG pads (1:3 or 1:4 ratio)
        # 5. Validate pad ring completeness
        pass
    
    def generate_pad_ring(self, pad_placements):
        # Generate complete pad ring structure:
        # - Corner cells
        # - I/O cells
        # - PG cells
        # - Filler cells
        pass
```

**交付物**:
- `src/analysis/pad_planner.py` (~500 lines)
- `src/analysis/pad_constraints.py` (~200 lines)
- `tests/test_pad_planner.py` (~350 lines)
- 示例: `configs/FullSystem/pad_plan.yaml`

---

## 5. Macro 摆放 (SRAM/Analog) (src/analysis/macro_placer.py)

**目标**: 自动规划 hard macro（SRAM、PLL、ADC/DAC 等）的摆放

**现有基础**:
- `src/analysis/floorplan_advisor.py` - 基础 macro placement 建议

**需要新增**:
- Macro 分类（digital SRAM, analog PLL/ADC, custom IP）
- Placement constraint 管理（keepout, halo, channel）
- Timing-driven placement（critical macros 靠近相关 logic）
- Routing congestion 预估（macro 间通道宽度）
- Power domain 约束（同 domain macros 聚集）
- Analog macro 特殊处理（isolation, substrate noise）

**关键算法**:
```python
class MacroPlacer:
    def place_macros(self, macro_list, core_area, constraints):
        # 1. Classify macros (SRAM, analog, digital IP)
        # 2. Apply hard constraints:
        #    - Analog macros at chip edge (isolation)
        #    - SRAM near memory controller
        #    - PLL near clock input
        # 3. Optimize for:
        #    - Timing (critical path length)
        #    - Routing (minimize congestion)
        #    - Power (group by domain)
        # 4. Generate placement with keepout/halo
        # 5. Validate no overlap + min spacing
        pass
    
    def generate_macro_constraints(self, macro_placements):
        # Generate placement constraints:
        # - Region constraints (macro must be in region)
        # - Guide constraints (macro should be near guide)
        # - Fence constraints (logic cannot enter fence)
        pass
```

**交付物**:
- `src/analysis/macro_placer.py` (~600 lines)
- `src/analysis/macro_constraints.py` (~250 lines)
- `tests/test_macro_placer.py` (~400 lines)
- 文档: `docs/MACRO_PLACEMENT_GUIDE.md`

---

## 6. DRC/LVS 修复 (src/analysis/drc_lvs_fixer.py)

**目标**: 自动分析 DRC/LVS 错误，生成修复建议或 ECO 脚本

**现有基础**:
- `src/analysis/error_checker.py` - 基础错误检查
- `src/parsers/calibre_parser.py` - Calibre 结果解析

**需要新增**:
- DRC error 分类（spacing, width, density, antenna 等）
- LVS error 分类（short, open, device mismatch, property 等）
- 自动修复策略（routing layer change, via insertion, cell swap）
- ECO 脚本生成（spare cell 使用、buffer insertion）
- Fix priority 排序（timing impact + fix complexity）
- Iterative fix flow（fix → re-check → fix next）

**关键算法**:
```python
class DRCLVSFixer:
    def analyze_errors(self, drc_db, lvs_report):
        # 1. Parse DRC errors by type and layer
        # 2. Parse LVS errors by type (short/open/device)
        # 3. Classify fixability:
        #    - Auto-fixable (spacing, density, antenna)
        #    - Semi-auto (shorts with clear path)
        #    - Manual (device mismatch, complex shorts)
        # 4. Generate fix priority list
        pass
    
    def generate_fix_script(self, errors, fix_strategy):
        # Generate ECO script:
        # - For spacing: adjust routing/via
        # - For antenna: insert diode
        # - For shorts: ripup and reroute
        # - For opens: add connection
        # - For device mismatch: swap cell
        pass
    
    def iterative_fix(self, max_iterations=5):
        # Iterative fix flow:
        # 1. Run DRC/LVS
        # 2. Analyze errors
        # 3. Apply fixes
        # 4. Re-run DRC/LVS
        # 5. Repeat until clean or max iterations
        pass
```

**交付物**:
- `src/analysis/drc_lvs_fixer.py` (~700 lines)
- `src/analysis/eco_script_gen.py` (~400 lines)
- `tests/test_drc_lvs_fixer.py` (~450 lines)
- 文档: `docs/DRC_LVS_FIX_METHODOLOGY.md`

---

## 实施顺序与依赖

```
Phase 1 (基础能力):
  1. Area Planner (无依赖，其他模块需要)
  2. Power Mesh Builder (依赖 Area Planner)

Phase 2 (设计拆分):
  3. Harden Engine (依赖 Partition Engine)
  4. Macro Placer (依赖 Area Planner)

Phase 3 (Pad 与验证):
  5. Pad Planner (依赖 Area Planner + PG Network)
  6. DRC/LVS Fixer (依赖现有 parsers)

Phase 4 (集成与测试):
  7. 端到端集成测试
  8. 文档完善
  9. 示例项目 (FullSystem with all features)
```

---

## 预估工作量

| 模块 | 代码量 | 测试量 | 文档 | 预估时间 |
|------|--------|--------|------|----------|
| Power Mesh | 650 lines | 300 lines | 2 pages | 2h |
| Harden Engine | 900 lines | 400 lines | 3 pages | 3h |
| Area Planner | 400 lines | 250 lines | 1 page | 1.5h |
| Pad Planner | 700 lines | 350 lines | 2 pages | 2.5h |
| Macro Placer | 850 lines | 400 lines | 3 pages | 3h |
| DRC/LVS Fixer | 1100 lines | 450 lines | 4 pages | 4h |
| **Total** | **4600 lines** | **2150 lines** | **15 pages** | **16h** |

---

## 成功标准

1. 所有新模块通过单元测试（>90% coverage）
2. 端到端流程可运行（从 RTL 到 GDS）
3. 支持 Synopsys + Cadence 双工具链
4. 完整文档（API + methodology）
5. 示例项目可复现

---

## 下一步

确认计划后，按 Phase 顺序开始实施：
1. 创建 `plan0626.md` ✅
2. 实现 Area Planner
3. 实现 Power Mesh Builder
4. ...
