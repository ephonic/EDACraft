// Auto-generated by rfsim_codegen. DO NOT EDIT.
#include "nmos_sh_gen.h"
#include <cmath>
#include <cstring>
#include <cstdlib>

// VA max/min 的 C++ 实现（避免 std::max/min 与 Windows 宏冲突）
static inline double rfsim_va_max(double a, double b) { return a > b ? a : b; }
static inline double rfsim_va_min(double a, double b) { return a < b ? a : b; }

namespace rfsim {

Nmos_shGenModel::Nmos_shGenModel(const std::string& name,
                       const std::vector<NodeId>& nodes,
                       const ParamList& instanceParams,
                       const ParamList& modelParams)
    : name_(name), numTerminals_(4) {
    nodes_.assign(4, 0);
    nodes_[0] = (nodes.size() > 0) ? nodes[0] : 0;  // port d
    nodes_[1] = (nodes.size() > 1) ? nodes[1] : 0;  // port g
    nodes_[2] = (nodes.size() > 2) ? nodes[2] : 0;  // port s
    nodes_[3] = (nodes.size() > 3) ? nodes[3] : 0;  // port b
    // Read model parameters
    for (const auto& [pn, pv] : modelParams) {
        if (pv.kind == ParamValue::Kind::Number) setModelParam(pn, pv.num);
    }
    // Read instance parameters
    for (const auto& [pn, pv] : instanceParams) {
        if (pv.kind == ParamValue::Kind::Number) setInstanceParam(pn, pv.num);
    }
    // Build Jacobian sparsity pattern
    jacPattern_.emplace_back(0, 0);
    jacPattern_.emplace_back(0, 1);
    jacPattern_.emplace_back(0, 2);
    jacPattern_.emplace_back(0, 3);
    jacPattern_.emplace_back(2, 0);
    jacPattern_.emplace_back(2, 1);
    jacPattern_.emplace_back(2, 2);
    jacPattern_.emplace_back(2, 3);
}

void Nmos_shGenModel::allocateInternalNodes(NodeId& internalNodeBase) {
}

void Nmos_shGenModel::setModelParam(const std::string& name, double value) {
    if (name == "vth0") { vth0_ = value; given_vth0_ = 1.0; return; }
    if (name == "kp") { kp_ = value; given_kp_ = 1.0; return; }
    if (name == "gamma") { gamma_ = value; given_gamma_ = 1.0; return; }
    if (name == "phi") { phi_ = value; given_phi_ = 1.0; return; }
    if (name == "lambda") { lambda_ = value; given_lambda_ = 1.0; return; }
}

void Nmos_shGenModel::setInstanceParam(const std::string& name, double value) {
    setModelParam(name, value);
}

void Nmos_shGenModel::stamp_pattern(StampPattern& out) const {
    for (const auto& [r, c] : jacPattern_) {
        if (r < nodes_.size() && c < nodes_.size())
            out.entries.emplace_back(nodes_[r], nodes_[c]);
    }
}

void Nmos_shGenModel::eval(const OperatingPoint& op, DeviceContribution& out) const {
    // Node voltages (indexed by local node index)
    const auto& op_v = op.v;
    // Initialize residual
    out.f.assign(4, 0.0);
    out.jac.assign(jacPattern_.size(), 0.0);

    double myadms_t1, myadms_t2;
    double vgs;
    double vds;
    double vbs;
    double vth;
    double vgsat;
    double id;
    double beta;
    double Vb = op_v[nodes_[3]];
    double Vd = op_v[nodes_[0]];
    double Vg = op_v[nodes_[1]];
    double Vs = op_v[nodes_[2]];
    const double _der0 = 0.0;  // DC: ddt conductance factor
    double dVbDv3 = 1.;
    double dVdDv0 = 1.;
    double dVgDv1 = 1.;
    double dVsDv2 = 1.;
    double didDv0;
    double didDv1;
    double didDv2;
    double didDv3;
    double dvbsDv2;
    double dvbsDv3;
    double dvdsDv0;
    double dvdsDv2;
    double dvgsDv1;
    double dvgsDv2;
    double dvgsatDv1;
    double dvgsatDv2;
    double dvgsatDv3;
    double dvthDv2;
    double dvthDv3;
    double dVbDv0 = 0.;
    double dVbDv1 = 0.;
    double dVbDv2 = 0.;
    double dVdDv1 = 0.;
    double dVdDv2 = 0.;
    double dVdDv3 = 0.;
    double dVgDv0 = 0.;
    double dVgDv2 = 0.;
    double dVgDv3 = 0.;
    double dVsDv0 = 0.;
    double dVsDv1 = 0.;
    double dVsDv3 = 0.;
    double contributetmp = 0.;
    double dcontributetmpDv0 = 0.;
    double dcontributetmpDv1 = 0.;
    double dcontributetmpDv2 = 0.;
    double dcontributetmpDv3 = 0.;
    out.f[0] = 0.;
    out.f[1] = 0.;
    out.f[2] = 0.;
    out.f[3] = 0.;
    vgs = Vg-Vs;
    dvgsDv1 = dVgDv1;
    dvgsDv2 = (-dVsDv2);
    vds = Vd-Vs;
    dvdsDv0 = dVdDv0;
    dvdsDv2 = (-dVsDv2);
    vbs = Vb-Vs;
    dvbsDv2 = (-dVsDv2);
    dvbsDv3 = dVbDv3;
    vth = vth0_+gamma_*(tanh(sqrt(phi_-vbs))-tanh(sqrt(phi_)));
    dvthDv2 = gamma_*(1-tanh(sqrt((phi_-vbs)))*tanh(sqrt((phi_-vbs))))*(1/(2*sqrt((phi_-vbs)))*((-dvbsDv2)));
    dvthDv3 = gamma_*(1-tanh(sqrt((phi_-vbs)))*tanh(sqrt((phi_-vbs))))*(1/(2*sqrt((phi_-vbs)))*((-dvbsDv3)));
    beta = kp_;
    dvgsatDv1 = dvgsatDv2 = dvgsatDv3 = 0.0;
    if(vgs<=vth){
    id = 0;
    didDv0 = didDv1 = didDv2 = didDv3 = 0.0;
    } else {
    vgsat = vgs-vth;
    dvgsatDv1 = dvgsDv1;
    dvgsatDv2 = (dvgsDv2-dvthDv2);
    dvgsatDv3 = (-dvthDv3);
    if(vds>=vgsat){
    id = 0.5*beta*vgsat*vgsat*(1.0+lambda_*vds);
    didDv0 = 0.5*beta*vgsat*vgsat*lambda_*dvdsDv0;
    didDv1 = (0.5*beta*vgsat*dvgsatDv1+0.5*beta*dvgsatDv1*vgsat)*(1.0+lambda_*vds);
    didDv2 = (0.5*beta*vgsat*vgsat*lambda_*dvdsDv2+(0.5*beta*vgsat*dvgsatDv2+0.5*beta*dvgsatDv2*vgsat)*(1.0+lambda_*vds));
    didDv3 = (0.5*beta*vgsat*dvgsatDv3+0.5*beta*dvgsatDv3*vgsat)*(1.0+lambda_*vds);
    } else {
    id = beta*((vgsat-0.5*vds)*vds)*(1.0+lambda_*vds);
    didDv0 = (beta*(vgsat-0.5*vds)*vds*lambda_*dvdsDv0+beta*((vgsat-0.5*vds)*dvdsDv0+(-0.5*dvdsDv0)*vds)*(1.0+lambda_*vds));
    didDv1 = beta*dvgsatDv1*vds*(1.0+lambda_*vds);
    didDv2 = (beta*(vgsat-0.5*vds)*vds*lambda_*dvdsDv2+beta*((vgsat-0.5*vds)*dvdsDv2+(dvgsatDv2-0.5*dvdsDv2)*vds)*(1.0+lambda_*vds));
    didDv3 = beta*dvgsatDv3*vds*(1.0+lambda_*vds);
    }
    }
    myadms_t1 = id;
    out.f[0] += myadms_t1;
    dcontributetmpDv0 = didDv0;
    dcontributetmpDv1 = didDv1;
    dcontributetmpDv2 = didDv2;
    dcontributetmpDv3 = didDv3;
    for (size_t ji = 0; ji < jacPattern_.size(); ++ji) {
        if (jacPattern_[ji].first == 0 && jacPattern_[ji].second == 0)
            out.jac[ji] += dcontributetmpDv0;
    }
    for (size_t ji = 0; ji < jacPattern_.size(); ++ji) {
        if (jacPattern_[ji].first == 0 && jacPattern_[ji].second == 1)
            out.jac[ji] += dcontributetmpDv1;
    }
    for (size_t ji = 0; ji < jacPattern_.size(); ++ji) {
        if (jacPattern_[ji].first == 0 && jacPattern_[ji].second == 2)
            out.jac[ji] += dcontributetmpDv2;
    }
    for (size_t ji = 0; ji < jacPattern_.size(); ++ji) {
        if (jacPattern_[ji].first == 0 && jacPattern_[ji].second == 3)
            out.jac[ji] += dcontributetmpDv3;
    }
    myadms_t1 = (-id);
    out.f[2] += myadms_t1;
    dcontributetmpDv0 = (-didDv0);
    dcontributetmpDv1 = (-didDv1);
    dcontributetmpDv2 = (-didDv2);
    dcontributetmpDv3 = (-didDv3);
    for (size_t ji = 0; ji < jacPattern_.size(); ++ji) {
        if (jacPattern_[ji].first == 2 && jacPattern_[ji].second == 0)
            out.jac[ji] += dcontributetmpDv0;
    }
    for (size_t ji = 0; ji < jacPattern_.size(); ++ji) {
        if (jacPattern_[ji].first == 2 && jacPattern_[ji].second == 1)
            out.jac[ji] += dcontributetmpDv1;
    }
    for (size_t ji = 0; ji < jacPattern_.size(); ++ji) {
        if (jacPattern_[ji].first == 2 && jacPattern_[ji].second == 2)
            out.jac[ji] += dcontributetmpDv2;
    }
    for (size_t ji = 0; ji < jacPattern_.size(); ++ji) {
        if (jacPattern_[ji].first == 2 && jacPattern_[ji].second == 3)
            out.jac[ji] += dcontributetmpDv3;
    }
}

void Nmos_shGenModel::evalTransient(const TransientOpPoint& op, DeviceContribution& out) const {
    // No transient state, degrade to DC
    OperatingPoint dcOp{op.v};
    eval(dcOp, out);
}

} // namespace rfsim
